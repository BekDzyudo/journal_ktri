/**
 * Email orqali kelgan havola uchun yangi parol sahifasi.
 * `ResetPassword` bilan bir xil — token query parametri: `?token=...`
 * (masalan: /new-password?token=... yoki /reset-password?token=...)
 */
export { default } from "./ResetPassword";
